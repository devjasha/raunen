# built binary
/raunen

# release artefacts, uploaded to GitHub releases rather than committed
/dist/

# session store, if anyone points XDG at the checkout
/sessions/

# companion written by tests that point XDG at the checkout
/.raunen-test/

# Vercel build output, produced by the deploy rather than committed
/web/.vercel/
raunen-mcpauth
